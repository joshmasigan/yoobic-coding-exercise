import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'yoobic-coding-exercise',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
